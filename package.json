{
  "name": "zod-sst-demo",
  "version": "0.1.0",
  "scripts": {
    "dev": "nodemon src/index.ts",
    "build": "rimraf ./dist && tsc"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "^20.11.24",
    "nodemon": "^3.1.0",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2"
  }
}
